# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs


GET     /                                       App.Index
GET     /login                                  App.Login
GET     /logout                                 App.Logout
GET     /register                               App.Register
GET     /card                                   App.Card
GET     /deck                                   App.GetDeck
GET     /matches                                App.Matches
GET     /match/*id                              App.Match
GET     /admin                                  App.Admin
GET     /admin/match/new                        App.NewMatch
GET     /admin/users                            App.AdminUsers
GET     /mycards                                App.MyCards
PUT     /admin/match/:matchId/new_black_card    App.MatchNewBlackCard
PUT     /admin/match/:matchId/end_voting        App.EndVoting
PUT     /match/:matchId/pick_card/:cardId       App.PickCard
PUT     /match/:matchId/vote_card/:cardId       App.VoteCard


GET     /matches/join/*id                       App.JoinMatch

POST    /login                                  App.PostLogin
POST    /register                               App.PostRegister

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")